{
  "name": "worker",
  "version": "1.0.0",
  "description": "Worker service to generate thumbnails asynchronously.",
  "scripts": {
    "build": "tsc",
    "start": "ts-node-dev --poll -P tsconfig.json src/index.ts"
  },
  "author": "Black",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.254.0",
    "amqplib": "^0.10.3",
    "dotenv": "^16.0.1",
    "jimp": "^0.16.13",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.1",
    "@types/node": "^17.0.30",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.2.4"
  },
  "engines": {
    "node": ">=4.18.1"
  }
}
